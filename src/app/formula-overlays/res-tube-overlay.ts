import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * res_tube sheet formula overlay
 * Contains formula implementations for the res_tube sheet
 */
export class ResTubeOverlay implements FormulaOverlay {
  /**
   * Apply res_tube formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement res_tube sheet formulas
  }
}