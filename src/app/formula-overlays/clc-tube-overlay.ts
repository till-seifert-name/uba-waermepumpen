import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * clc_tube sheet formula overlay
 * Contains formula implementations for the clc_tube sheet
 */
export class ClcTubeOverlay implements FormulaOverlay {
  /**
   * Apply clc_tube formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement clc_tube sheet formulas
  }
}