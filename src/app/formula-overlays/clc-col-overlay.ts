import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * clc_col sheet formula overlay
 * Contains formula implementations for the clc_col sheet
 */
export class ClcColOverlay implements FormulaOverlay {
  /**
   * Apply clc_col formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement clc_col sheet formulas
  }
}