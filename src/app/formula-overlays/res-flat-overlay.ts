import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * res_flat sheet formula overlay
 * Contains formula implementations for the res_flat sheet
 */
export class ResFlatOverlay implements FormulaOverlay {
  /**
   * Apply res_flat formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement res_flat sheet formulas
  }
}