import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * res_col sheet formula overlay
 * Contains formula implementations for the res_col sheet
 */
export class ResColOverlay implements FormulaOverlay {
  /**
   * Apply res_col formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement res_col sheet formulas
  }
}