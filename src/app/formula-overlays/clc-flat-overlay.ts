import { DataGrid } from '../data-grid';
import { FormulaOverlay } from './base-overlay';

/**
 * clc_flat sheet formula overlay
 * Contains formula implementations for the clc_flat sheet
 */
export class ClcFlatOverlay implements FormulaOverlay {
  /**
   * Apply clc_flat formulas to the data grid
   * @param grid The DataGrid instance to apply formulas to
   */
  applyFormulas(grid: DataGrid): void {
    // TODO: Implement clc_flat sheet formulas
  }
}